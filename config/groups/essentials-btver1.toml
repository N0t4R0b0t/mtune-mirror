# Package group: "essentials-btver1" — tuning targets specific to the AMD C-60
# machine (Radeon HD 6290, modern mesa/radeonsi). Enabled only on "btver1".
#
# Currently just the GL driver (modern mesa, unlike the Atom's mesa-amber). Flesh
# this out from an inspection of the actual C-60 box (its DE, apps, workload).

name        = "essentials-btver1"
description = "AMD C-60 machine-specific tuning (modern mesa)"

packages = [
  "mesa",
]

# Per-arch EXTRAS for "atom" (i686): packages built for this arch beyond its
# enabled groups (see config/arches/atom.toml `groups`), plus source overrides.
# Machine-specific tuning targets live in the essentials-atom group; this file is
# for one-offs. See config/packages/btver1.toml for field semantics.

[[package]]
name   = "pkgmirror-hello"
source = "local"
